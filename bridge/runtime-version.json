{
  "version": "1.9.1",
  "channel": "main",
  "notes": "V1.9.1 adds bounded self-healing for transient operator failures, state repair for Food Network jobs, temporary-file cleanup, SQLite quick-check diagnostics, crash backoff and dependency-aware rollback recovery. Manual Facebook login/CAPTCHA/2FA/checkpoints remain fail-safe and are never bypassed."
}